spring.application.name=Week07 Assignment Tester
server.port=8080

# Allow files up to 2 MB (student source files are tiny, but be generous)
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB

# Thymeleaf
spring.thymeleaf.cache=false
